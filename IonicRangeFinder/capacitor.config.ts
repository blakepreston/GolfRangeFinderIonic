import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'tech.blakepreston.golfrangefinder',
  appName: 'Golf Range Finder',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
